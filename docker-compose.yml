services:
  db:
    image: postgres:latest
    container_name: dpp-postgres
    environment:
      POSTGRES_DB: grants-dpp
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
    ports:
      - "5435:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
